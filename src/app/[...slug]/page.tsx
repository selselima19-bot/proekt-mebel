/*
Этот файл обрабатывает любые неизвестные адреса сайта.
Он нужен для того, чтобы пользователь не видел страницу 404 при переходе по ссылкам без готовой страницы.
Вместо ошибки пользователь автоматически возвращается на главную страницу.
*/

import { redirect } from "next/navigation";

export default function CatchAllPage() {
  // Если адрес не найден среди существующих страниц, переводим пользователя на главную.
  redirect("/");
}

